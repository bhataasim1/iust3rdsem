#include <stdio.h>

int
main ()
{
  int bs, da, hr, gs;
  printf ("Enter the Basic Salery", bs);
  scanf ("%d", &bs);

  da = 0.40 * bs;
  hr = .20 * bs;
  gs = da + hr;
  printf ("Gross Salery is %d", gs);

  return 0;
}
