#include <stdio.h>

int main()
{
    int c,d,temporary;
    printf("Enter the First Number ",c);
    scanf("%d",&c);
    
    printf("Enter the Second Number ",d);
    scanf("%d",&d);
    
    temporary=c;
    c=d;
    d=temporary;
    printf("First Number after Interchanging %d \n",c);
    printf("Second Number after Interchanging %d",d);

    return 0;
}
